function openResume() {
    // Open the resume PDF file from the local storage/public folder
    const resumeUrl = '/Krishnaraj.pdf';  // Update with the correct path to your resume
    window.open(resumeUrl, '_blank');
  }